import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    sponsor: {
        backgroundColor: 'red',
        width: '100%',
        height: 100,
    }
});

export default styles;
